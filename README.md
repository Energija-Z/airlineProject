Used tech stack is PHP, with Symfony framework, to deploy this structure.

Simply pull this git repo (git pull origin master) and write symfony server:start, which will enable this site to be used at https://127.0.0.1 endpoint.

It took every day at least two hours to create this design.